<%= form_with(model: attachment) do |form| %>
  <% if attachment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
        <% attachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :chat_session_id, style: "display: block" %>
    <%= form.text_field :chat_session_id %>
  </div>

  <div>
    <%= form.label :file_url, style: "display: block" %>
    <%= form.text_field :file_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
