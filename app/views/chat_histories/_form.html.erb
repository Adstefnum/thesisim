<%= form_with(model: chat_history) do |form| %>
  <% if chat_history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chat_history.errors.count, "error") %> prohibited this chat_history from being saved:</h2>

      <ul>
        <% chat_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :chat_session_id, style: "display: block" %>
    <%= form.text_field :chat_session_id %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.textarea :content %>
  </div>

  <div>
    <%= form.label :reply_to_id, style: "display: block" %>
    <%= form.text_field :reply_to_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
